{
  "name": "seo-spider",
  "version": "0.2.7",
  "private": true,
  "description": "Advanced Website Spider & SEO Auditor — High-performance cloud-native crawler for serious SEO audits.",
  "license": "UNLICENSED",
  "author": "manassankhla",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "tsc -b && npm run build:next",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,css,html}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md,css,html}\"",
    "test": "npm run test --workspaces --if-present",
    "build:web": "tsc -b && npm run build:next",
    "start:web": "npm run start:next",
    "build:next": "npm run build --workspace apps/web",
    "start:next": "npm run start --workspace apps/web"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.20.0"
  }
}
